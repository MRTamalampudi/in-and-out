import React, { FC } from 'react';
import styles from './owe-form.module.scss';

interface OweFormProps {}

const OweForm: FC<OweFormProps> = () => (
  <div className={styles.OweForm}>
    OweForm Component
  </div>
);

export default OweForm;
