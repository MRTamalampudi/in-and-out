import {BaseService} from "./base-service";

class BillsGroupService implements BaseService<number>{
    create(data: number): number {
        return 0;
    }

    delete(id: number): void {
    }

    get(id: number): number {
        return 0;
    }

    index(query: string): number[] {
        return [];
    }

    update(data: number): number {
        return 0;
    }

}